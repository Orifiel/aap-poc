---
- name: Get pods
  hosts: all
  connection: local
 
  tasks:
    - name: Show me the pods
      ansible.builtin.debug:
        var: pod_list

    - name: Get pods
      ansible.builtin.command: oc get pods