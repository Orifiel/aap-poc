---
- name: Get pods
  hosts: all
  connection: local
  tasks:
    - name: Get pods
      ansible.builtin.command: oc get pods
      register: pod_list

    - name: Show me the pods
      ansible.builtin.debug:
        var: pod_list

    