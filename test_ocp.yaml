---
- name: Get pods
  hosts: all
  connection: local
  vars: 
    api_url: "{{ lookup('env', 'K8S_AUTH_HOST') }}"
  tasks: 
    - name: teste
      shell: echo $K8S_AUTH_HOST
      register: teste

    - name: teste2
      debug:
        var: api_url
  
    
                 
  
    #- name: Auth to cluster
    #  ansible.builtin.command: 
    #    cmd: "oc login --token={{ api_token }} --server={{ url_api }}"

    #- name: Get pods
    #  ansible.builtin.command: "oc get pods"
    #  register: pod_list

    #- name: Show the pods
    #  ansible.builtin.debug:
    #    var: pod_list
